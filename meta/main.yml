galaxy_info:
  author: Sabareeswaran
  description: Ansible role for [describe purpose here]
  company: sabaree@comaony  # Optional

  # Issue tracker URL (if not on GitHub, uncomment and update)
  # issue_tracker_url: http://example.com/issue/tracker

  # Choose a valid license from https://spdx.org/licenses/
  license: MIT  # Change as needed (e.g., GPL-2.0-or-later, Apache-2.0)

  min_ansible_version: 2.9  # Set based on your role requirements

  # Ansible Galaxy tags (remove '[]' if adding tags)
  galaxy_tags:
    - ansible
    - automation
    - [other tags related to the role]

dependencies:
  # List role dependencies if any
  # Example:
  # - role: common
  #   version: v1.0.0
